<?php

/**
 * Implements hook_ctools_plugin_api().
 */
function services_shipping_ctools_plugin_api($owner, $api) {
    if ($owner == 'services' && $api == 'services') {
        return array(
            'version' => 3,
            'file' => 'services_shipping.services.inc'
        );
    }
}
